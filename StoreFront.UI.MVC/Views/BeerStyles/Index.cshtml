@model IEnumerable<StoreFront.DATA.EF.BeerStyle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BeerStyle1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BeerStyle1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BeerStyleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BeerStyleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BeerStyleID })
        </td>
    </tr>
}

</table>
